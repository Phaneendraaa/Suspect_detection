version: "3.9"

services:
  backend:
    build: ./backend
    container_name: backend
    ports:
      - "8001:8001"
    restart: always

  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always